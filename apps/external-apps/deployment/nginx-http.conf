# http-context capacity for long exact publication names (up to 64 x 100).
# Install only if these directives are not already owned by another host config;
# otherwise review/update the existing values instead of creating duplicates.
server_names_hash_bucket_size 256;
server_names_hash_max_size 8192;
