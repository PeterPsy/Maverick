{
  "tools": {
    "senses_operations_manifest": {
      "description": "Describe Senses Phase 6 backend surfaces, dependency requirements, frontend, and runtime routing work.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false
      }
    },
    "senses_reference_manifest": {
      "description": "Report the Senses reference surface. Capture reference search remains deferred after Phase 6 routing.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false
      }
    },
    "senses_view_filter": {
      "description": "Read the current Senses workspace-shared frontend view state. Requires an authenticated Maverick actor.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false
      }
    },
    "senses_set_view_filter": {
      "description": "Set Senses tab, query, and status filters. Requires workspace-admin authority because the state is workspace-shared.",
      "input_schema": {
        "type": "object",
        "properties": {
          "view_filter": {
            "type": "object"
          },
          "tab": {
            "type": "string"
          },
          "query": {
            "type": "string"
          },
          "status": {
            "type": "string"
          },
          "device_id": {
            "type": "string"
          },
          "capture_id": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    "senses_set_custom_view": {
      "description": "Set a curated Senses custom view. Requires workspace-admin authority because the state is workspace-shared.",
      "input_schema": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string"
          },
          "refs": {
            "type": "array"
          }
        },
        "additionalProperties": false
      }
    },
    "senses_clear_custom_view": {
      "description": "Return Senses to normal browsing. Requires workspace-admin authority because the state is workspace-shared.",
      "input_schema": {
        "type": "object",
        "additionalProperties": false
      }
    }
  }
}
